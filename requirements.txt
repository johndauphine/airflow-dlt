# Installed into the Airflow image (see Dockerfile).
# Keep in sync with [project.dependencies] in pyproject.toml.

apache-airflow==3.0.0
apache-airflow-providers-celery>=3.10.0
apache-airflow-providers-postgres>=5.12.0
dlt[postgres,sql_database]>=1.5.0
pyodbc>=5.0.0
pydantic>=2.6
pyyaml>=6.0
redis>=5.0.0

# Dev
pytest>=8.0.0
